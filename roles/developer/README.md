# developer role

For machines that host interactive development shells, agent harnesses, and
operator terminal sessions.

The role currently reuses the generic headless WezTerm server profile at
[`shared/editor/wezterm/server.lua`](../../shared/editor/wezterm/server.lua).
Pinned binaries, client connection names, install locations, and persistence
evidence remain host-specific.
