# linux role

For Linux machines managed through system and user service managers. Long-lived
processes require a checked-in unit or drop-in, an install-path mapping, a reload
or restart procedure, and a verification command.

No Linux configuration has been promoted to `shared/` yet. The current user PATH
and service-manager exceptions are evidence-backed `proximal` overrides, not a
infrastructure default.
