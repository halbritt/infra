# windows role

For Windows machines managed through the Service Control Manager. Desired state
must record the service wrapper or native service, startup and recovery policy,
firewall scope, install paths, and PowerShell verification commands.

No Windows configuration has been promoted to `shared/` yet.
