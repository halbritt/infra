# Baseline — accepted GUC set

The current accepted, non-default configuration for `proximal:5432`, with a
one-line rationale for each value tied to a measurement or a report under
`reports/`. Default-valued GUCs are not listed.

> Not yet populated. The first `POSTGRES_TUNING` inventory run captures the live
> `pg_settings` snapshot into `inventory/`; promote the deliberately-set,
> non-default values here with their rationale once reviewed.

| parameter | value | source | rationale | evidence |
|---|---|---|---|---|
| _(none yet)_ | | | | |
