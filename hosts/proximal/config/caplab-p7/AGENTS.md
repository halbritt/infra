# CAPLAB P7 host integration

Read `README.md` before changing or enacting this subsystem. CAPLAB product,
study, analysis, and result authority remains in `/home/halbritt/git/caplab`;
this directory owns only the non-secret P7 desired state for `proximal`.

Never commit or print a Garage secret, credential file, raw Study 001 byte,
internal object locator, or recomputation output. The only live identity is the
temporary `caplab_reader`. It may read the exact P6 registration, Garage
bucket, and independent copy. This surface must not enable the writer or
verifier, mutate P6 state, infer capability, decide eligibility, export data,
call a model, train, publish, route, or accept CAPLAB.

Repository checks and Stage A implementation authority do not authorize live
installation or reader enablement. Enact the runbook only after a separate
CAPLAB decision binds the exact clean CAPLAB and Proximal commits.
