# Principal Instruction — standing policy signature and follow-through (2026-08-31, r2)

Source: council topic `new-predicate`, floor review #196–#212, synthesis
`synthesis-2026-08-31-comprehensive-report-review.md`, prompt contributions #216 (agy),
#217 (sol), #218 (deepseek), #219 (glm). Execute in order. All delegated acts listed on the
ledger with reasons; all principal-owned acts executed verbatim from the signed text below
and listed as principal-owned under this instruction record. **Record this instruction on
the ledger before the first policy landing.**

## §0 Constraints (standing)

- The freeze guard stays armed. No `main` write outside the paths this instruction opens:
  the standing policy below (once materialized) and the personal acceptances named in §3/§4.
- No further ad-hoc exception brackets. Do not convert any refusal into an exception not
  expressly authorized here; if a write qualifies under neither the standing policy nor a
  signed act below, stop and surface it.
- The policy covers **code-class only**. Catalog, registry, rendered-policy, schema,
  semantic-environment, and constitutional writes still route to me or the train — no
  exceptions by interpretation.
- No pre-scripted resolutions; every escalation is read individually.
- Killed or externally terminated gate/check processes read failed-or-unknown, never absent.
- Report numbers and record ids, not summaries.

## §1 Standing drain-deadlock policy — SIGNED (principal-owned)

I adopt the standing drain-deadlock policy as drafted in the 2026-08-30 evening report §5.3,
**as corrected by the council record** (variance-not-void, sol #189/#217; dossier-predates-
merge, glm #192; sunset-at-guard, script-computed bills, and policy-ref-in-authority-proof,
fable/glm #219). Signing the uncorrected draft is not authorized. I authorize you to
materialize the following as a ledger record and policy artifact verbatim:

> **Standing drain-deadlock policy (enacted 2026-08-31).** While any live frontier prevents
> ordinary integration to `main`, a branch may merge and deploy without a per-instance
> Principal grant iff ALL of:
> (1) its gate is green at the merged tip, with the gate exit captured on that tip;
> (2) independent review is complete with all findings dispositioned — no partial-review
> merges;
> (3) its consequence class, computed from the deploy-surface diff, is **code**: rendered
> policy, catalog, registry, decision records, schema, and the semantic environment are
> untouched;
> (4) its computed consequence bill is ≤ N re-stamps and **0 regenerations**, where N
> follows the signed derivation rule — largest code-class bill on record + 1, floor 3
> (today N = 3); semantic-environment bills never enter N's calibration;
> (5) it either unwedges a live frontier merge or delivers a defect fix whose dossier
> **predates the merge record**;
> (6) the merge record is written **before** the write, in the RQ-323669 form, and carries:
> the exact deploy-surface diff, the consequence class and bill **as the diff script's
> output, never prose**, the affected-frontier set, the recovery successor for each
> affected frontier, and a reference to this policy record.
>
> Every landing under this policy lists the policy record's ref in its authority proof.
>
> **Refusal floor:** a blocked or uncomputable consequence is refused to every authority
> kind — this policy prices computed consequences; it never waives computation.
>
> **Variance, not void:** if a landing's actual consequence exceeds its recorded bill, the
> merge remains history (the ledger is append-only); the variance immediately suspends
> further use of this policy, writes a variance/retrospective record, and triggers a
> docketed recovery route for the affected frontiers. Use resumes only after the variance
> is adjudicated.
>
> **Sunset:** this policy retires automatically when `maintenance-has-a-computed-
> successor@1` registers its priced-admission guard; from that point the mechanical path
> governs.

Materialize this record first; every subsequent merge in this instruction cites it or a
personal act — no third form.

## §2 cpw@1 — from planned to run (delegated, first priority after §1)

RQ-333941 is issued and planned (334080). Drive its first delivery packet to an actual lane
run. **Done-when: a packet run exists on the ledger with a run id and a `pass_run_opened`
record under RQ-333941** — planned is not delivered. The next report opens with that run id
or the specific refusal that blocked it. Do not let mhcs or policy landings displace it.

## §3 Governance sitting — draft for my personal acceptance (delegated drafting; personal acts)

Prepare one batched amendment package for RFC 0010/0016 and stage it for my acceptance
(non-delegable, per the amendment's own terms). Prepared, not executed: nothing in this
section merges or takes effect until I personally accept it. It carries, in one sitting:

1. **By-content continuity authority ratification** — the 08-31 fix #1 (`83083103119e`)
   touches D0010-owned semantics; ratify the by-content-hash resolution of personal
   verdicts.
2. **D0016.C5 pair-completion rule** (council-converged, #196–#212, exact wording): a
   personal rejection that is one half of a **completed** continuity pair pins the
   acceptance position personal only until its linked successor occurrence is personally
   accepted; thereafter the position is delegable for new content. A substantive personal
   rejection with no linked accepted successor keeps the forever-pin. The pin scopes to the
   adjudicated declaration tuple/content, not the identity.
3. **IR-acceptance proxy classification**: name which kind the standing IR-acceptance proxy
   is under the two-kind model — a delegation record (delegate, scope, gates, expiry, my
   words verbatim) or per-act principal-owned instruction — and materialize the record it
   lacks.
4. **Decoy-candidate lifecycle** (already queued, report §7.1).
5. **P12 renewal**: the delegation set in `policy/gates.yaml` expires **2026-09-21**. Draft
   the renewal delegation records now with explicit scope and new expiry; stage for my
   acceptance in the same sitting. **Add a dated countdown row, with a decision deadline of
   2026-09-14, to the top of every handoff until signed.**

Report the package as: file paths, the exact acceptance acts required of me, and nothing
merged until I accept.

## §4 mhcs Phase-2 (staged behind §2's witness)

After §2's packet run exists, stage `mhcs-registration@8c3bd40` (rdd@1 rides it) for my
Phase-2 signatures and personal acceptance, so its priced-admission guard can replace and
sunset the §1 policy. List the signature items exactly as the registration branch defines
them, including the enforcement-point claims (`make deploy`, `semantics/current`;
`--no-verify`, remote pushes, and web edits stay unclaimed, listed, and fail-closed). The
ordering is deliberate: non-displacement must be visible in the sequencing, not asserted.

## §5 B1(ii) — decide from data (delegated evaluation, my clause call)

Evaluate the first full post-`drain-fold-hoist` window (drain now 50.6s/14.6s). Report:
bounce count by class, longest clean stretch, and whether the pre-registered closure
condition is met. I will mark the clause from that readout only.

## §6 Hygiene and record fixes (delegated)

1. Cycle off `session-12.scope` (abandoned 08-19, 101.1G peak) at the next safe boundary.
2. Fix the comprehensive report's §4 pointer ("see §6" → §7.1) and mark any superseded rows
   inline per the standing convention.
3. Keep the wake exit-2 freshness race on the watch list; if it recurs near a courtesy
   drive, file it with the resolution-delta family and dossier it first — which is what
   qualifies its fix for §1 clause (5). Dossier, then merge.

## §7 Report back

Open the next report with, in order:
1. the §1 policy record id;
2. the cpw@1 first packet run id (with its `pass_run_opened` record) or the blocking
   refusal;
3. **every §1-policy landing: computed bill vs. actual, side by side** — this pair is both
   the policy's integrity check and the N-recalibration series for mhcs Phase 1;
4. the §3 package paths and the exact acts required of me, plus the P12 countdown row;
5. the B1(ii) readout;
6. any unresolved refusals, verbatim.
