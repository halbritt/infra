Principal, 2026-08-31 ~11:5x PDT, interactive multiple-choice adjudication of the merge/deploy timing for the accepted governance transaction (sitting record RQ-340088). Three selections, verbatim:

1. Timing: "First train after withholding-a integrates (Recommended)" — materialize the 04:30 PDT train now; the one-transaction merge/deploy (branch governance-sitting-2026-08-31@377c0a0 + branch mhcs-registration@8c3bd40 + the standing-drain-deadlock policy artifact + D0010/D0016 decision-record regeneration + deploy) lands at the first 04:30 epoch after the withholding-is-explainable-a standing-guards packet has integrated to main (mechanical condition: the three withholding-guards member checks read delivery_status != red in origin/main policy/checks/repository.json), with hard fallback date 2026-09-07 regardless of frontier state.

2. Decision-record grant: "Pre-accept both, this bracket only (Recommended)" — the D0013-form grant: acceptance of the deterministic decision-generation OUTPUTS for D0010 and D0016, produced over the merged tree in this one train transaction, is pre-accepted; never a hand-edit; lint-rfcs verifies the body equals the deterministic projection; every other decision record's acceptance stays personally reserved.

3. Unattended execution: "Confirmed, as described (Recommended)" — the train executes unattended at 04:30 PDT: verifies the condition mechanically, computes the consequence projection by script, writes the merge record BEFORE the write (RQ-323669 form, citing RQ-340088/RQ-339830; the drain-deadlock policy RQ-339831 does not price this semantic-environment write and is cited only as context), merges the two accepted branches past the freeze guard with git merge --no-verify (scoped strictly to executing the already-personally-accepted transaction), regenerates D0010/D0016 via tools/decision-gen, runs the full check, deploys, pushes, and closes with an observation. On ANY failure it aborts fail-closed: restores the checkout to origin/main, deploys nothing, preserves every log and partial artifact, writes a failure report, and notifies the Principal. Killed or terminated steps read failed-or-unknown, never absent.
