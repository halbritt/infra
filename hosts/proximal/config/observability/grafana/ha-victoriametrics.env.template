# Install as /etc/grafana/ha-victoriametrics.env (0600 root, outside Git).
# Use the query-only vmauth-homeassistant credential, never the writer.
VM_HA_READ_USER=homeassistant_reader
VM_HA_READ_PASSWORD=CHANGE_ME
