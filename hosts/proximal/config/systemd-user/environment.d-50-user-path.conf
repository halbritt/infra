# Striatum LLM lanes (and any user unit) must resolve the user-installed
# claude (~/.local/bin, v2.x) ahead of the stale root install at
# /usr/local/bin (v1.0.60, retired default model). 2026-07-07.
PATH=/home/halbritt/.local/bin:/home/halbritt/.npm-global/bin:$PATH
