{
  "//": "Canonical wigolo MCP registration for any client on proximal. Synthesis is wired to native gemini-3.6-flash (@google/genai path) as of 2026-08-06 — free tier, low volume; watch for charges. Replace <Gemini key> with GEMINI_API_KEY from ~/.config/ai-newsroom/env (leftover there; wigolo is its only consumer). Previous GLM 5.2/OpenRouter block: see git history or CHANGELOG 2026-07-21. To run fully keyless/on-box, drop the three env vars (host or core-only synthesis).",
  "mcpServers": {
    "wigolo": {
      "command": "/home/halbritt/.npm-global/bin/wigolo",
      "args": [],
      "env": {
        "WIGOLO_LLM_PROVIDER": "gemini",
        "GEMINI_API_KEY": "<Gemini key — GEMINI_API_KEY from ~/.config/ai-newsroom/env>",
        "WIGOLO_LLM_MODEL": "gemini-3.6-flash"
      }
    }
  }
}
