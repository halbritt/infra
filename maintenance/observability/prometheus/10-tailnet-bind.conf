# proximal observability: these bind tailnet IP 100.85.100.81, which exists only after tailscaled
# brings the interface up. Wait for it and self-heal if the bind races startup.
[Unit]
Wants=network-online.target
After=network-online.target tailscaled.service

[Service]
Restart=on-failure
RestartSec=5
