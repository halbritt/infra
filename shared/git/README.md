# Shared Git configuration

No Git configuration has been promoted here yet. Add a file only after its
identity-free settings and semantics are proven reusable across hosts. User
names, email addresses, signing keys, credential helpers, and repository tokens
are not shared configuration.
