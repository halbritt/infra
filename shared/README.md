# Shared configuration

This directory contains configuration whose bytes and meaning are reusable
across machines. A role declares each shared input that it consumes.

Promotion into `shared/` requires a second real consumer or another concrete
reason to centralize policy. Similar host files remain separate until that
boundary is earned.
