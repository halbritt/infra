# Shared WezTerm server profile

[`server.lua`](server.lua) is the platform-neutral headless mux profile used by
the `developer` role. The `proximal` installer consumes it from the shared path;
the pinned Ubuntu build and client profile remain under the host.
