# Shared editor and terminal configuration

Reusable editor and terminal fragments. Machine-specific connection names,
binary pins, installation scripts, and hardware workarounds remain under the
host that owns them.
